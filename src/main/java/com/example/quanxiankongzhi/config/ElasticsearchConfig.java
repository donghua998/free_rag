package com.example.quanxiankongzhi.config;
import org.springframework.context.annotation.Configuration;
@Configuration
public class ElasticsearchConfig {
    // Spring Boot 自动配置，无需额外配置
    // 如需自定义，可在这里添加 RestHighLevelClient 等 Bean
}
